/**
 * controllers/dashboardController.js
 *
 * Placeholder stub — same 501 response as before.
 */

export async function getStats(req, res) {
  return res.status(501).json({
    message: "Not implemented: GET /api/dashboard/stats",
  });
}
